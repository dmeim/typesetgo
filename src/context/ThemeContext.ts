// Compatibility entry point; context identity is independent of provider refreshes.
export { ThemeContext } from "@/context/theme-context";
export type { ThemeContextValue, ThemeSelectionOptions } from "@/context/theme-context";
export { ThemeProvider } from "@/context/ThemeProvider";
